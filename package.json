{
  "name": "video-trimmer-pro",
  "version": "4.0.0",
  "description": "Preview and trim videos entirely in the browser using MediaRecorder — no FFmpeg.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": { "start": "node server.js" },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
